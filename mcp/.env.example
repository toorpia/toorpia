# toorPIA API configuration (used when running the server directly, e.g. `npm run dev`)
# In normal use these are set in the MCP client's configuration instead.
TOORPIA_API_KEY=your_api_key_here
# Optional. Defaults to the hosted service.
TOORPIA_API_URL=https://api.toorpia.com
